# Preceded by type, paths follow (one or more for rename)
struct open {
    fd: fd;
    unit: u16 hex;
    version_cycle: u8 hex;
    eflag: u8 hex;
    nid: nid;
    pid: pid;
    sflag: u16 hex;
    oflag: u16 hex;
    mode: u16 hex;
    zero: u32 hex;
}

struct status {
    status: u16;
}